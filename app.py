import streamlit as st

# إعداد الواجهة
st.set_page_config(page_title="المستشار القانوني", layout="wide")

st.title("⚖️ المستشار القانوني الذكي")
st.write("ديوان عام منطقة البحيرة")

# المدخلات
question = st.text_area("اطرح استفسارك القانوني:")

if st.button("تحليل"):
    if question:
        # هنا سنضع المنطق الذي سيبحث في نصوص ملفاتك
        st.write("🔍 جاري الفحص في قواعد البيانات القانونية...")
        
        # مثال للرد الذي سيظهر بمجرد اكتمال الربط
        st.success("النتيجة:")
        st.markdown("""
        **المادة القانونية:** بناءً على ما ورد في القانون...
        **التحليل:** بناءً على الحالة...
        **القرار:** يستحق / لا يستحق.
        """)
    else:
        st.warning("يرجى كتابة السؤال.")

st.sidebar.info("مع تحيات وليد حماد - الإدارة العامة للشئون القانونية")
